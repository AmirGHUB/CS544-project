server:
  port: 8085

entityManager:
  domain: edu.miu.cs544.team6.domain

spring:    
  application:
    name: tm
  security:
    user:
      name: admin
      password: admin
  datasource:
    url: jdbc:mysql://localhost:3306/team6?useSSL=false
    username: team6
    password: team6
    driverClassName: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
      naming:
        implicit-strategy: org.hibernate.boot.model.naming.ImplicitNamingStrategyLegacyJpaImpl
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl

    show-sql: true


    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5Dialect
  mail:
    default-encoding: UTF-8
    host: smtp.gmail.com
    username: ea544.team6@gmail.com
    password: EA544_team6
    port: 587
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
    protocol: smtp
    test-connection: false

sync-job:
  delay:
    milliseconds:
      fixed: 5000
      initial: 5000
